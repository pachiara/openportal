<!-- header -->
    <nav class="white" role="navigation">
      <div class="navbar white navbar-default navbar-fixed-top">
        <div class="navbar-header">
          <a id="logo" class="navbar-brand" href="<%= root_path %>"><%= image_tag current_site.the_logo, class: "img-responsive" %></a>
          <button data-target="#navbar-main" data-toggle="collapse" type="button" class="navbar-toggle">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>
        <div id="navbar-main" class="navbar-collapse collapse">
          <div class="green-text nav navbar-nav navbar-left">
            <h4><b><%= current_site.name %>&nbsp;&nbsp;&nbsp;</b></h4>
          </div>
          <h5><%= raw get_nav_menu('main_menu', 'nav navbar-nav') %></h5>
          <%= render partial: 'partials/search_form' %>
          <ul class="nav navbar-nav navbar-right">
            <% if signin?  %>
            <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#"><img src="<%= current_user.decorate.the_avatar %>" style="max-width: 36px; border-radius: 50%" alt=""/><%= current_user.username %></a>
              <ul class="dropdown-menu">
                <li><a href="<%= admin_dashboard_path %>">Dashboard</a></li>
                <li><a href="<%= admin_profile_path %>">Profile</a></li>
                <li><a href="<%= admin_logout_path %>">Logout</a></li>
              </ul>
            </li>
            <% else %>
            <li class="dropdown">
              <a id="login-user" class="dropdown-toggle" data-toggle="dropdown" href="#">Login / Register</a>
              <ul class="dropdown-menu">
                <li><a href="<%= admin_login_path %>">Login</a></li>
                <li><a href="<%= admin_register_path %>">Register</a></li>
              </ul>
            </li>
            <% end %>
          </ul>
          <%= raw current_site.draw_languages("language list-inline pull-right", true) %>
        </div>
      </div>
    </nav>
    <nav class="green" role="navigation">
    </nav>      

 <!-- ./header -->     	   	